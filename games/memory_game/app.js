import cardArray from "./cardArray";

document.addEventListener("DOMContentLoaded", () => {
  const grid = document.querySelector(".grid");
  const score = document.querySelector("#score");

  const cardChosen = [];
  const cardChosenId = [];
  const cardsWon = [];

  const createBoard = () => {
    // create cells, add emoji and then hide emoji
    // attach event handler that looks up specific card
  };

  const isMatch = () => {};

  const revealCard = () => {};
  // show card for a timeOut, check if match, and then show as completed if match
  //
});
